# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo
 
# Environment secrets — never commit real credentials
.env
env/.env.*.user
 
# Runtime state (created decision records live here, not in the seed)
data/decisions.runtime.json
 
# Agents Toolkit local artifacts
appPackage/build/
.localConfigs*
devTools/
 
# Misc
.DS_Store
*.log